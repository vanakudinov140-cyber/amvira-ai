# Retention CRM — production на Amvera (Docker, порт 80)
# https://docs.amvera.ru/applications/configuration/docker.html

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 80
