# Admin dashboard — отдельный проект Amvera (корень: admin-dashboard/)
# API backend: https://retention-v2-ivankudinov.amvera.io
# После деплоя URL: https://<имя-проекта>.amvera.app (например retention-admin-ivankudinov)

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 80
  servicePort: 80
