# Standalone frontend — корень репозитория (без monorepo root path)
# API: https://retention-v2-ivankudinov.amvera.io

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 80
  servicePort: 80
