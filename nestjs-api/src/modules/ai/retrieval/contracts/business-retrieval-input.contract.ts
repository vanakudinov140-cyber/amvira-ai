import type { AiContextAssemblyInput } from '../../context/ai-context.builder';

/**
 * Read-only input for retrieval — same surface as context assembly (no transport).
 */
export type BusinessRetrievalInput = AiContextAssemblyInput;
