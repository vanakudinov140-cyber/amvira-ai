meta:
  environment: node
  toolchain:
    name: npm
    version: 10

build:
  commands:
    - npm install
    - npm run build

run:
  command: npm run start:prod

  containerPort: 3000

  env:
    - name: NODE_ENV
      value: production

    - name: PORT
      value: 3000