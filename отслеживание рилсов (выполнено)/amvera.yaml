# Документация: https://docs.amvera.ru/applications/configuration/docker.html
meta:
  environment: docker
  toolchain:
    name: docker

run:
  # Постоянное хранилище: сюда пишется bot.db и экспорт Excel
  persistenceMount: /data
